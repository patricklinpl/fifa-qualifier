{
  "name": "web-scraper-template",
  "version": "1.0.0",
  "description": "template web scraper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel src -s -D -d dist",
    "start": "npm run build && node dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patricklinpl/web-scraper-template.git"
  },
  "author": "Patrick Lin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patricklinpl/web-scraper-template/issues"
  },
  "homepage": "https://github.com/patricklinpl/web-scraper-template#readme",
  "dependencies": {
    "axios": "^0.18.0",
    "cheerio": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "babel": {
    "presets": [
      "env",
      "stage-0"
    ]
  }
}
